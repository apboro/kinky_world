import React from 'react';
import style from './aboutUsPage.module.css';
export const AboutUsPage = (props) => {
	return (
		<div className={`container`}>
			<h1 className={`${style.about}`}>Info Block</h1>
		</div>
	);
};
